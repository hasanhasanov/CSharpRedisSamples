@{
    ViewBag.Title = "Questions";
}
@section featured {
    <section class="featured">
        <div class="content-wrapper">
            <hgroup class="title">
                <h1>@ViewBag.Title :</h1>
            </hgroup>
            <p>
                @ViewBag.Message
            </p>
        </div>
    </section>
}
